# Awesome Jev Projects Radar
> Curated directory and ecosystem radar of 134 real-world open-source projects powered by TypeSafe AI's Jev model.

- Website: https://logicrw.github.io/awesome-jev-projects/
- GitHub Repository: https://github.com/logicrw/awesome-jev-projects
- Maintained by: @0xLogicrw (https://x.com/0xLogicrw)

## About Jev
Jev (by TypeSafe AI) is a specialized low-latency decision model designed for structured choices, ratings, classification, and next-step actions (sub-100ms inference). Rather than generating long text, Jev acts as a fast decision co-processor alongside heavy frontier LLMs.

## Curated Projects Directory (134 Projects)

### jev-ultrafast
- Repository: https://github.com/browser-use/jev-ultrafast
- Category: Browser & OS Action
- Overview (EN): Ultra-fast browser agent using Jev for per-step DOM action decisions. Complete Google Flights search in ~7.1s.
- Overview (ZH): 给浏览器一个目标，Jev 负责选按钮和动作，文字交给小模型写。作者的 Google Flights 示例约 7.1 秒完成。
- Jev Decision Role: 读当前 DOM，在一次请求里选择操作和目标元素；需要输入时才调用文本模型。
- Core Benefit: 把界面选择与文字生成分开，减少重复读页面。

### typesafe-mcp
- Repository: https://github.com/itsmostafa/typesafe-mcp
- Category: MCP & Integrations
- Overview (EN): MCP server connecting Jev directly into Claude Code, Claude Desktop, and Codex as a decision co-processor.
- Overview (ZH): 给 Claude Code、Claude Desktop 和 Codex 接上一个选择题外脑：选哪个、打几分、是不是，都用同一个 MCP 工具问。
- Jev Decision Role: 把状态和 typed questions 交给 Jev，返回 Choice、Score、Noul 及概率。
- Core Benefit: 拿到可以直接写进分支判断的答案，省去解析一大段文字。

### jev-mcp
- Repository: https://github.com/jkudish/jev-mcp
- Category: MCP & Integrations
- Overview (EN): Pragmatic MCP utility suite packaging real-time fact-checking, prompt injection guards, and semantic reranking.
- Overview (ZH): 给 Agent 配三个小助手：核对引用、筛查注入指令、从候选里挑出相关结果。
- Jev Decision Role: 通过 verify、screen、find 分别给证据支持度、内容风险和候选相关性做判断。
- Core Benefit: 让每次读取和引用都能加一道可配置的检查。

### SemDecide
- Repository: https://github.com/sharziki/semdecide
- Category: CLI & Pipelines
- Overview (EN): Unix command-line utility bringing Jev into terminal pipes and CI pipelines for semantic filtering and scoring.
- Overview (ZH): 给终端管道加一点语义判断：一句话分类、一批文本过滤，都能接在 Bash 和 CI 后面。
- Jev Decision Role: 对输入做分类或概率判断，再由本地规则执行阈值、退出码与 allow/escalate/block。
- Core Benefit: 不用搭一个完整 Agent，也能把语义判断嵌进现有脚本。

### Jev Codex Router
- Repository: https://github.com/0xNatoshi/jev-codex-router
- Category: Routing & Cost Optimization
- Overview (EN): Smart request router evaluating turn difficulty with Jev to route between cheap and frontier models.
- Overview (ZH): 每轮先看任务有多难，再给 Codex 选模型和思考深度。简单活省一点，难活再上强模型。
- Jev Decision Role: Jev 判断当前轮次的任务层级与推理需求，本地策略决定模型、effort 和速度档。
- Core Benefit: 作者对 237 轮历史会话按标价重算，估计比全用最贵模型省约 60%。

### Winnow
- Repository: https://github.com/GhalebDweikat/winnow
- Category: Context GC & Filter
- Overview (EN): Context garbage collector for Claude Code pruning voluminous bash, grep, and file outputs.
- Overview (ZH): 给 Claude Code 的长日志装一道筛子。暂时不相关的内容先藏起来，想看时还能完整找回。
- Jev Decision Role: 逐块判断 Read、Bash、Grep 输出是否有用；保留相关或不确定内容，隐藏高置信度无关块。
- Core Benefit: 减少进入上下文的冗余输出，并保留可召回的原文。

### Jev Review
- Repository: https://github.com/devagrawal09/jev-review
- Category: Codebase & Graph Pathfinding
- Overview (EN): Code review triage engine assessing correctness, security, reliability, and compatibility before deep review.
- Overview (ZH): 先给代码变更分诊，再沿着风险线索往下查。结果放在本地面板里，方便逐条看证据。
- Jev Decision Role: 分阶段评估风险、选择文件和证据、判定机制与严重程度，再决定审查路径。
- Core Benefit: 把审查收敛到具体代码片段和风险信号。

### Blink
- Repository: https://github.com/ellipsis-dev/blink
- Category: Codebase & Graph Pathfinding
- Overview (EN): Semantic pathfinder navigating large codebases without vector indexes using beam search.
- Overview (ZH): 描述你要找的代码，让一群探针顺着目录往下走。看起来越相关的路径，分到的探针越多。
- Jev Decision Role: 给文件和目录名判断相关概率，按概率把 walkers 分配到下一层。
- Core Benefit: 直接沿文件树查找，无需先建立向量索引。

### neo4jev
- Repository: https://github.com/jexp/neo4jev
- Category: Codebase & Graph Pathfinding
- Overview (EN): Knowledge graph pathfinder scoring candidate edges with Jev and traversing paths via beam search.
- Overview (ZH): 在 Neo4j 里一步步找关系：每到一个节点，就问 Jev 下一条边该往哪走。
- Jev Decision Role: Choice 为相邻关系分配概率，Noul 判断是否到达目标；本地 beam search 保留候选路径。
- Core Benefit: 把自然语言目标接到可查看的图谱路径上。

### jev-desktop
- Repository: https://github.com/lahfir/agent-desktop
- Category: Browser & OS Action
- Overview (EN): jev-desktop: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 把电脑里的按钮和菜单交给 Jev 来选。它读原生无障碍结构，一步步完成桌面操作。
- Jev Decision Role: Jev 同时选择动作与目标，并评估置信度和操作风险；本地策略决定是否执行或停止。
- Core Benefit: 让主 Agent 不必把整棵界面树塞进上下文。

### TypeSafe AI Playground
- Repository: https://github.com/markjaquith/typesafe-ai-playground
- Category: CLI & Pipelines
- Overview (EN): Rust CLI playground experimenting with PHI detection, code comment review, and sentiment classification.
- Overview (ZH): 一个 Rust 实验箱：查隐私信息、看代码注释、判断表达是否友善，直接在终端试 Jev。
- Jev Decision Role: 把短任务拆成概率或类别判断，例如是否含 PHI、注释是否有用、语气是否友善。
- Core Benefit: 用可运行的小实验理解结构化判断能放在哪里。

### Prism
- Repository: https://github.com/irfndi/prism-liquidity-agent
- Category: Domain & Vertical Tools
- Overview (EN): DeFi liquidity agent detecting toxic flow, market stress, and pool distribution in shadow mode.
- Overview (ZH): 观察 Solana 流动性池的 Agent。Jev 在旁边给风险打分，先与规则结果对照，不直接决定买卖。
- Jev Decision Role: 对入池分布、毒性交易流、回归持有与市场压力给出影子判断，日志用于校准。
- Core Benefit: 给既有规则多一组可比较的信号；交易决定仍由确定性规则控制。

### 1v1 Jev
- Repository: https://github.com/emrickgarrett/OneVOneJev
- Category: High-Frequency & Simulation
- Overview (EN): 1v1 Jev: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 在浏览器里和 Jev 打一局 1v1 射击。它看结构化战况，选走位、瞄准和开火。
- Jev Decision Role: 每个决策 tick 同时询问移动、视角、开镜、开火与跳跃；API 不可用时切到启发式逻辑。
- Core Benefit: 把结构化判断放进持续交互的游戏循环，直观看到决策过程。

### Safer with Jev
- Repository: https://github.com/andrelandgraf/typesafe-on-neon
- Category: Security & Guardrails
- Overview (EN): Serverless request router on Neon evaluating incoming queries and dispatching to specialized frontier models.
- Overview (ZH): 给 HTTP 请求装一道内容门禁。Jev 先检查注入指令或不安全内容，通过了再转发到指定地址。
- Jev Decision Role: 检查请求内容是否允许通过；本地代码在 pass 时转发，review 或 block 时拦住。
- Core Benefit: 把内容检查放到上游请求前，直接复用现有 HTTP 服务。

### fast-jev-compaction
- Repository: https://github.com/tamaratran/fast-jev-compaction
- Category: Context GC & Filter
- Overview (EN): fast-jev-compaction: Prunes extraneous tool outputs, logs, and grep results before feeding LLM context.
- Overview (ZH): 给 Claude Code 压缩上下文时，先删掉过时的工具调用和结果。留下来的原文不改写，关键对话仍按原样保留。
- Jev Decision Role: 分别判断一条工具调用是否还要记住、它的结果是否仍需完整保留；本地规则决定保留、截短或删除。
- Core Benefit: 用原文筛选替代部分摘要，减少路径、报错和命令在改写中走样。

### jev-review
- Repository: https://github.com/NiazMorshed2007/jev-review
- Category: Codebase & Graph Pathfinding
- Overview (EN): Code review triage engine assessing correctness, security, reliability, and compatibility before deep review.
- Overview (ZH): 给写代码的 Agent 一块质量仪表盘：每改一段，就看看可读性、安全性和测试等维度有没有变好。
- Jev Decision Role: 接收聚焦的代码差异与上下文，用 Score、Choice、Noul 给各质量维度打分，再与上一轮比较。
- Core Benefit: 把每轮修改的质量变化摆出来，帮助主 Agent 决定接下来查哪里。

### pg-jev
- Repository: https://github.com/realZachi/pg-jev
- Category: Data & Search
- Overview (EN): pg-jev: Semantic query and classification layer applied over tabular databases and search indexes.
- Overview (ZH): 让 SQL 直接问「哪些工单的客户正在生气」。用一句人话给数据库行筛选、分类和排序。
- Jev Decision Role: 把行内容成批交给 Jev，返回是否符合条件、所属类别或等级分数，供 WHERE、GROUP BY、ORDER BY 使用。
- Core Benefit: 语义条件可以和原有 SQL 一起写，并复用同一会话里已经算过的判断。

### jevpilot
- Repository: https://github.com/standardagents/jevpilot
- Category: High-Frequency & Simulation
- Overview (EN): jevpilot: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 在浏览器里开一辆小车，让 Jev 从提前算好的路线和速度里选下一步。
- Jev Decision Role: 读取路况、附近车辆和候选轨迹，选择转向与速度；碰撞预测和紧急刹车由代码处理。
- Core Benefit: 把驾驶判断和物理计算拆开，能直接查看每次选择及其概率。

### jev-browser
- Repository: https://github.com/jkudish/jev-browser
- Category: Browser & OS Action
- Overview (EN): jev-browser: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 给它一个网址和任务，它会选按钮、填字段、翻页面，最后交回页面内容和每一步的操作记录。
- Jev Decision Role: 每步用 Choice 选 DOM 动作，用两个 Noul 判断是否完成或卡住；输入文字交给另一小模型。
- Core Benefit: 通过 MCP、CLI 或库复用同一浏览器循环，并查看动作、恢复过程和错误记录。

### pi-jev
- Repository: https://github.com/y0usaf/pi-jev
- Category: Security & Guardrails
- Overview (EN): pi-jev: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Pi 编程助手加一个风险观察员：执行前看命令会不会越界，执行后看日志是否泄露秘密、错误该怎么处理。
- Jev Decision Role: 先判断破坏性、外传、越界和影响，再给输出做秘密检测与错误分类；也提供可直接调用的 jev_ask。
- Core Benefit: 把操作风险和故障类型及时提示给 Agent 与用户。

### pi-jev-auto-mode
- Repository: https://github.com/jomatsu/pi-jev-auto-mode
- Category: Security & Guardrails
- Overview (EN): pi-jev-auto-mode: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Pi 的命令执行加一道门禁。明确安全的先通过，其余交给 Jev 判断；默认拿不准就拦住。
- Jev Decision Role: 本地规则先处理硬拒绝和允许项，Jev 再检查升级上来的 bash、write、edit 是否符合授权及风险条件。
- Core Benefit: 把常见允许规则与语义检查接起来，保留每次判断记录供调阈值。

### Jev-Trades
- Repository: https://github.com/zadescoxp/Jev-Trades
- Category: Domain & Vertical Tools
- Overview (EN): High-frequency perp trader on Hyperliquid with live dashboard (jev-trade.com). Evaluates order book every tick to trade.
- Overview (ZH): 用实时加密货币行情练习模拟交易。Jev 给出交易判断，面板展示虚拟仓位和技术指标，不连真实下单接口。
- Jev Decision Role: 读取已完成的分钟 K 线与技术指标，对启用的资产返回结构化交易判断；Python 按置信度和仓位限制更新模拟账户。
- Core Benefit: 把行情、模型信号和模拟持仓放在一起观察，便于检查决策过程。

### jev
- Repository: https://github.com/dannote/jev
- Category: MCP & Integrations
- Overview (EN): jev: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 把 Jev 变成 Elixir 程序里的一个异步同伴。发去状态和问题，答案回来后直接用模式匹配决定下一步。
- Jev Decision Role: 把 Noul、Choice、Score 的回复转成 Elixir map，再由 GenServer 的 handle_answer 与 guard 路由。
- Core Benefit: 沿用 OTP 的消息、监督和错误处理方式接入模型判断。

### jev-mcp
- Repository: https://github.com/blakestone-x/jev-mcp
- Category: MCP & Integrations
- Overview (EN): Pragmatic MCP utility suite packaging real-time fact-checking, prompt injection guards, and semantic reranking.
- Overview (ZH): 给 MCP 客户端加上分类、打分、是非判断和候选匹配工具。
- Jev Decision Role: 将工具参数转成 Jev 的 Choice、Score、Noul 问题，返回结构化答案和概率。
- Core Benefit: Agent 拿到能直接用于分支判断的结果，不用再解析一段回答。

### HA-Jev
- Repository: https://github.com/AboveColin/HA-Jev
- Category: Domain & Vertical Tools
- Overview (EN): HA-Jev: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 让智能家居回答「衣服是不是洗完却忘了拿」。Jev 的答案变成 Home Assistant 传感器，接进已有自动化。
- Jev Decision Role: 把选定设备和实体的状态交给 Jev，得到概率、选项或分数，再按配置阈值触发自动化。
- Core Benefit: 自然语言条件能复用现有传感器、通知和场景，不必另搭聊天助手。

### jev-benchmarks
- Repository: https://github.com/AbdelStark/jev-benchmarks
- Category: Decision Tools
- Overview (EN): jev-benchmarks: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 把 Jev 和 GLiNER 放到同一批分类题上，除了答对率，也检查概率靠不靠谱。
- Jev Decision Role: 对固定文本和标签集合做分类，记录每个标签的概率、耗时和失败。
- Core Benefit: 能看清模型在哪些任务上适合自动处理，在哪些任务上容易过度自信。

### Jevbridge
- Repository: https://github.com/gamesonrblx/Jevbridge
- Category: MCP & Integrations
- Overview (EN): Jevbridge: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 给现有 Agent 接一个结构化决策适配器：同样的问题可以交给 Jev，也可以交给普通模型或离线规则。
- Jev Decision Role: 对状态回答 Choice、Score、Noul，再按置信度把动作建议分成执行、确认、升级或放弃。
- Core Benefit: 统一决策接口，便于试验任务路由、工具门禁和界面动作选择。

### jev-playground
- Repository: https://github.com/mizchi/jev-playground
- Category: CLI & Pipelines
- Overview (EN): jev-playground: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 一个 MoonBit 实验场：让 Jev 下五子棋、打简化 MOBA、判断命令风险，也试着把判断写进小语言。
- Jev Decision Role: 把棋盘、游戏状态、命令或候选任务转成类型化问题，让 Jev 选动作或给风险评分。
- Core Benefit: 把不同问题的问法、结果和限制放到可重跑的小实验里比较。

### zod-jev
- Repository: https://github.com/jomatsu/zod-jev
- Category: MCP & Integrations
- Overview (EN): zod-jev: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 给 Zod 表单校验补上「意思对不对」这一层：字段格式过关后，再看描述是否匹配、内容是否含个人信息。
- Jev Decision Role: 把同一次解析的语义规则合并成 Jev Noul 问题，再按概率映射成通过、拒绝、不确定或不可用。
- Core Benefit: 继续使用熟悉的 Zod 错误对象，把语义问题定位到具体字段。

### jev-lm
- Repository: https://github.com/y0usaf/jev-lm
- Category: CLI & Pipelines
- Overview (EN): jev-lm: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 把下一个词当选择题，试着用 Jev 拼出句子；也能让它挑选本地草拟的整段续写。
- Jev Decision Role: Choice 选词，Noul 判断候选续写能否接上以及是否该停止。
- Core Benefit: 用一套小实验看清决策模型拿来生成文字时会遇到什么问题。

### jev-search
- Repository: https://github.com/superagents-lab/jev-search
- Category: Data & Search
- Overview (EN): jev-search: Semantic query and classification layer applied over tabular databases and search indexes.
- Overview (ZH): 用人话搜网页，先挑搜索源和时间范围，再把相关链接排到前面。页面给链接和摘要，不代写答案。
- Jev Decision Role: Jev 对请求做结构化判断以选择检索参数，再给 Search1API 返回的标题和摘要评分。
- Core Benefit: 把来源选择与结果排序串起来，同时保留可改的过滤条件和失败来源提示。

### live-jev
- Repository: https://github.com/vinilana/live-jev
- Category: High-Frequency & Simulation
- Overview (EN): live-jev: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 俯视小车模拟器：Jev 选车道和速度，还能与聊天模型跑同一条路线。
- Jev Decision Role: 一次请求判断变道、速度、危险程度和是否让行，代码再按阈值执行。
- Core Benefit: 把两种模型放进相同路况里，直接对照决策、耗时和用量。

### jev-code
- Repository: https://github.com/devagrawal09/jev-code
- Category: Codebase & Graph Pathfinding
- Overview (EN): jev-code: Semantic exploration engine for codebases and knowledge graphs using beam search.
- Overview (ZH): 给编程 Agent 一个小帮手：找相关文件、检查改动是否偏题、整理测试失败和审查意见。
- Jev Decision Role: 先在四个固定流程中选一个，再对有限的 diff、代码或日志片段做结构化判断；本地规则形成带位置的报告。
- Core Benefit: 把需要继续检查的线索和未检查范围一起返回，帮助 Agent 安排下一步。

### daf-jev
- Repository: https://github.com/docxology/daf-jev
- Category: MCP & Integrations
- Overview (EN): daf-jev: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 把 Jev 常用零件装成一个 Python 工具箱：提问、批量跑样本、看校准情况，再把结果接到程序或 MCP。
- Jev Decision Role: 构造 Noul、Choice、Score 问题，接收概率后由本地函数组合评分、路由及置信度门槛。
- Core Benefit: 同一套接口覆盖调用、失败记录和评估，便于比较问法与阈值。

### jev-chat
- Repository: https://github.com/adhyaay-karnwal/jev-chat
- Category: CLI & Pipelines
- Overview (EN): jev-chat: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 一个研究聊天解码器：不断让 Jev 选词或短语，再由代码接成回答。
- Jev Decision Role: 从分层候选表选下一段内容，或一次选定完整回复。
- Core Benefit: 把选答案和生成语言的差别变成可查看的实验记录。

### jev-cli
- Repository: https://github.com/tumf/jev-cli
- Category: CLI & Pipelines
- Overview (EN): jev-cli: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 在终端发一段文本或 JSON，直接拿回「是不是、选哪个、打几分」。输出能接进脚本继续处理。
- Jev Decision Role: 将单题或多题状态发送给 Jev，返回 Noul、Choice、Score 的完整 JSON 或主值。
- Core Benefit: 只用 Python 标准库提供输入、退出码和结构化错误，方便管道调用。

### AskJev
- Repository: https://github.com/ranjan2829/AskJev
- Category: Browser & OS Action
- Overview (EN): AskJev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 给浏览器装一个任务副驾。输入目标后自动点按钮、填表；碰到付款、删除等高风险操作时停下来确认。
- Jev Decision Role: 从当前网页的交互元素里选择下一步动作，并评估点击的风险与不可逆程度。
- Core Benefit: 把日常网页操作和高风险确认放进同一个扩展，也能通过 MCP 接给其他 Agent。

### jev-for-engineers
- Repository: https://github.com/Foadsf/jev-for-engineers
- Category: Domain & Vertical Tools
- Overview (EN): jev-for-engineers: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 机械与电气工程的八组 Jev 小实验：分派设计任务、检查仿真日志、匹配零件，再由 Python 规则决定怎么处理。
- Jev Decision Role: 对工程文本做分类、候选选择与风险判断；尺寸计算和最终处置仍交给普通代码。
- Core Benefit: 用合成样例看清工程判断如何接入程序；示例阈值需要用自己的数据重测。

### jev-system-one
- Repository: https://github.com/haseeb-heaven/jev-system-one
- Category: CLI & Pipelines
- Overview (EN): jev-system-one: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 在终端里提问：OpenAI 写回答，Jev 负责定回答方式、检查草稿并决定是否重写。
- Jev Decision Role: 先判断模式、深度与澄清需求，再检查草稿是否切题、有无无依据陈述。
- Core Benefit: 回答和决策报告并排显示，方便看模型为什么进入下一步。

### JevBird
- Repository: https://github.com/leftspace89/JevBird
- Category: High-Frequency & Simulation
- Overview (EN): JevBird: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 让 Jev 玩 Flappy Bird：代码先算候选飞行路线，再让模型选走哪条。
- Jev Decision Role: 每根管道通常问一次 Choice 选轨迹，同时用 Noul 判断危险；代码安排拍翅时间。
- Core Benefit: 不用每帧问模型，界面还能画出选中和被拒绝的路线。

### jev-judgment
- Repository: https://github.com/HyunjunJeon/jev-judgment
- Category: Security & Guardrails
- Overview (EN): jev-judgment: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给编程 Agent 增加一次判断检查：该不该问用户、命令是否越界、失败后还能不能重试。主模型继续负责写代码。
- Jev Decision Role: 根据对话和命令结果，回答授权是否明确、操作风险多大、失败属于哪一类。
- Core Benefit: 把需要停下确认的节点写清楚；模型给出的概率不能替代用户授权。

### jev-cli
- Repository: https://github.com/jtsang4/jev-cli
- Category: CLI & Pipelines
- Overview (EN): jev-cli: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 在终端里问 Jev 判断题。输入文本或 JSON，再给出分类、是非或评分问题，拿回脚本能直接读取的 JSON。
- Jev Decision Role: 针对同一份输入做分类、真假判断和分级评分，返回选项及其概率。
- Core Benefit: 能接收标准输入，把语义校验接进已有脚本和 CI；支持直连 TypeSafe 或走 Vercel 网关。

### claude-jev
- Repository: https://github.com/buchmark/claude-jev
- Category: Codebase & Graph Pathfinding
- Overview (EN): claude-jev: Semantic exploration engine for codebases and knowledge graphs using beam search.
- Overview (ZH): 给 Claude Code 的审查结论找个复核助手。把疑似 Bug、排错猜想、设计方案和搜索结果交给 Jev 打分，再决定先看什么。
- Jev Decision Role: 判断缺陷是否存在且可触发、哪个假设更符合症状，以及哪些文件与当前问题有关。
- Core Benefit: 把复查优先级变成可查看的分数；评分仍需配合读代码和实际复现。

### jev-browser
- Repository: https://github.com/tontoko/jev-browser
- Category: Browser & OS Action
- Overview (EN): jev-browser: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 让 Playwright 听懂网页操作指令。支持点按钮、填整张表和按记录提取信息，也能通过持久会话 CLI、MCP 或 SDK 使用。
- Jev Decision Role: 从真实页面元素与文本候选中选择操作目标、字段对应关系和要提取的值。
- Core Benefit: 动作来自页面候选，完成与否交给页面回读或明确断言，方便接入自动化测试。

### pi-jev
- Repository: https://github.com/TheoOliveira/pi-jev
- Category: MCP & Integrations
- Overview (EN): pi-jev: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 帮 Pi 编程 Agent 按任务找工具和技能。还提供结构化判断，并可选择开启模型分流与工具历史筛选。
- Jev Decision Role: 判断哪些未启用工具、技能和历史记录与当前任务有关，供本地规则决定加载或保留。
- Core Benefit: 让能力按需进入工作流；自动模式需主动开启，Jev 不可用时有本地或原生流程回退。

### jev-tree
- Repository: https://github.com/reachjalil/jev-tree
- Category: Classification & Taxonomy
- Overview (EN): jev-tree: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 选项太多，一次问不下？先把目录分成树，让 Jev 逐层选分支，最后落到具体商品、事件类型或工作流。
- Jev Decision Role: 每次只在当前层的候选分支里做单选，再沿选中的分支继续查找。
- Core Benefit: 不用直接截掉大目录尾部的候选，还能返回完整选择路径；失败时明确报告不可用。

### jev-starter
- Repository: https://github.com/hamakyo/jev-starter
- Category: SDK & Decision Frameworks
- Overview (EN): jev-starter: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Jev 判断补上应用里的后半段：把结果送去自动处理、备用模型或人工复核，并记录每条规则的效果。
- Jev Decision Role: 为分类或检查任务提供选项与概率；代码里的阈值决定后续处理路线。
- Core Benefit: 把业务规则留在代码里，便于评估误判、转人工比例和不同阈值的取舍。

### aside-jev
- Repository: https://github.com/himomohi/aside-jev
- Category: Browser & OS Action
- Overview (EN): aside-jev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 给 Aside 浏览器 Agent 接一个 Jev 决策助手。Agent 先列好可执行动作，Jev 选一个，再交给 Aside 操作网页。
- Jev Decision Role: 从应用提供的动作表里选择候选 ID，结合概率阈值决定执行、放弃或交给人确认。
- Core Benefit: 把选动作和执行动作分开，方便检查候选是否合法，并独立复查操作结果。

### jev-skill-gate
- Repository: https://github.com/ShivamPansuriya/jev-skill-gate
- Category: Context GC & Filter
- Overview (EN): jev-skill-gate: Prunes extraneous tool outputs, logs, and grep results before feeding LLM context.
- Overview (ZH): 技能装得多，先别把说明全塞给 Claude Code。它按当前项目给技能排相关度，保留有用的说明，其余只留名字或手动入口。
- Jev Decision Role: 结合项目技术栈、目录和 README，逐个判断已安装技能是否相关，再由规则调整可见程度。
- Core Benefit: 减少进入上下文的技能描述，同时保留手动调用入口；没有 API key 时可用本地评分。

### jev-guard
- Repository: https://github.com/leepokai/jev-guard
- Category: Security & Guardrails
- Overview (EN): jev-guard: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 在编程 Agent 调工具前后加一道检查：操作是否危险、是不是用户要求的、返回内容里有没有诱导 Agent 越界的指令。
- Jev Decision Role: 结合会话对工具风险、用户意图与提示注入迹象做判断，由本地规则决定放行、提醒或拦截。
- Core Benefit: 把风险检查接到多种 Agent 的工具流程；确认能力因客户端而异，也不能替代沙箱。

### jev-mcp
- Repository: https://github.com/rashedInt32/jev-mcp
- Category: MCP & Integrations
- Overview (EN): Pragmatic MCP utility suite packaging real-time fact-checking, prompt injection guards, and semantic reranking.
- Overview (ZH): 把 Jev 装成通用 MCP 判断工具。Agent 可以拿它做分类、打分、是非检查，也能一次提交多个问题。
- Jev Decision Role: 在调用方提供的选项和评分标准内作答，返回答案、概率分布及可供规则使用的判断信号。
- Core Benefit: 现有 MCP 客户端就能接入；带选项校验和弃权结果，方便处理不确定的回答。

### jevclient
- Repository: https://github.com/AboveColin/jevclient
- Category: SDK & Decision Frameworks
- Overview (EN): jevclient: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 在 Python 异步程序里调用 Jev 的小客户端。一次发出多个判断问题，直接拿到分类、分数和概率对象。
- Jev Decision Role: 围绕同一份状态回答是非、单选和评分问题，把结果交还 Python 程序继续处理。
- Core Benefit: 省去自己封装请求和解析回答的工作；可复用 aiohttp 连接，重试策略由调用方掌握。

### jev-workbench
- Repository: https://github.com/molis-ai/jev-workbench
- Category: MCP & Integrations
- Overview (EN): jev-workbench: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 在本地网页里定义、试跑并发布 Jev 判断函数，后端和 Agent 调用同一个固定版本。
- Jev Decision Role: 把工单分类、证据判断等问题保存为 Noul、Choice 或 Score 函数，再调用 TypeSafe。
- Core Benefit: 同一套判断不用在每个 Agent 或后端里重写，调用方版本也能固定。

### got-jev
- Repository: https://github.com/phureewat29/got-jev
- Category: Domain & Vertical Tools
- Overview (EN): got-jev: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 《权力的游戏》文字冒险：大模型写剧情，Jev 判断地点、情绪和危险，再切换配乐与背景。
- Jev Decision Role: 对新剧情判断 location、beat、mood、danger、inFiction 五项状态。
- Core Benefit: 用固定状态驱动界面和下一回合，不必从生成的故事里硬拆字段。

### pi-fast-jev-compaction
- Repository: https://github.com/joelhooks/pi-fast-jev-compaction
- Category: CLI & Pipelines
- Overview (EN): pi-fast-jev-compaction: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 给 Pi 删掉过时的工具调用和结果，保留原话，不另外生成摘要。
- Jev Decision Role: 对每个工具调用分别问是否保留调用与结果，再执行保留、截短结果或成对删除。
- Core Benefit: 减少送给主模型的旧工具内容，并保留可重建的裁剪记录。

### hermes-jev-approvals
- Repository: https://github.com/anpicasso/hermes-jev-approvals
- Category: MCP & Integrations
- Overview (EN): hermes-jev-approvals: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): Hermes 命令审批插件：让 Jev 选通过、拒绝或交给人看，再由代码应用本机策略。
- Jev Decision Role: 一次判断审批结果、策略覆盖、影响范围、敏感信息与外发行为。
- Core Benefit: 把只需要三个结果的审批环节接到专门的选择接口。

### jev-research
- Repository: https://github.com/sherajdev/jev-research
- Category: Decision Tools
- Overview (EN): jev-research: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 一份 Jev 与 Herdr 协作指南，附带把任务分给不同 Agent 的小原型。
- Jev Decision Role: 根据任务和仓库状态选择执行者、判断风险和是否准备好派发。
- Core Benefit: 给多 Agent 调度提供一个能看懂、能改的起点。

### jev-resilience
- Repository: https://github.com/Vicente-MD/jev-resilience
- Category: MCP & Integrations
- Overview (EN): jev-resilience: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 给 Spring WebFlux 检查“HTTP 200 但正文其实报错”的响应。
- Jev Decision Role: Jev 判断响应正文是否是隐藏错误或维护通知，超过阈值就抛出业务异常。
- Core Benefit: 让现有错误处理能看到状态码掩盖的失败。

### jev-go
- Repository: https://github.com/Stumble/jev-go
- Category: MCP & Integrations
- Overview (EN): jev-go: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 社区 Go SDK 和交互命令行，既能直连 TypeSafe，也能走 Vercel AI Gateway。
- Jev Decision Role: 用 Go 类型构建 Choice、Noul、Score 请求，解析并校验返回值。
- Core Benefit: Go 项目可以直接接入 Jev，并处理超时、重试和请求取消。

### jev-agent-failure-benchmark
- Repository: https://github.com/TokenTrim/jev-agent-failure-benchmark
- Category: Decision Tools
- Overview (EN): jev-agent-failure-benchmark: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给失败的多 Agent 记录找原因：哪个 Agent、哪一步、哪种错误。
- Jev Decision Role: 每条轨迹问三道 Choice，分别选择责任 Agent、关键步骤和错误类型。
- Core Benefit: 把故障归因做成可重复评分的实验，保留失败请求与结果。

### computer-use-jev
- Repository: https://github.com/paulsmith/computer-use-jev
- Category: Browser & OS Action
- Overview (EN): computer-use-jev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 用 Go 控制 macOS 应用，让 Jev 从当前可访问性树里选控件和下一步操作。
- Jev Decision Role: 根据窗口快照选择动作、目标、是否需要输入文字及任务是否完成。
- Core Benefit: 控件候选来自实时界面，模型只选已发现的对象。

### jev-broadcast-lab
- Repository: https://github.com/4anti/jev-broadcast-lab
- Category: Domain & Vertical Tools
- Overview (EN): jev-broadcast-lab: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 一个以国际象棋为主的 Jev 实验台，也能试工单分类、文档匹配和审核。
- Jev Decision Role: 从 chess.js 算出的合法走法中选择一步；Stockfish 分数只给操作者看。
- Core Benefit: 可以同时观察模型选步和本地棋力评估，检查两者的差别。

### jevgo
- Repository: https://github.com/fgn/jevgo
- Category: MCP & Integrations
- Overview (EN): jevgo: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 一个只依赖 Go 标准库的 Jev 客户端，可选接上 Langfuse 记录调用。
- Jev Decision Role: 发送结构化状态和三类问题，返回带类型的答案，并检查响应是否符合请求。
- Core Benefit: 把连接、校验、取消和观测接入 Go 服务。

### jev-little-airways
- Repository: https://github.com/lbotinelly/jev-little-airways
- Category: High-Frequency & Simulation
- Overview (EN): jev-little-airways: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 小岛机场模拟器：让 Jev 判断飞机是否改降、盘旋、让行，以及谁先落地。
- Jev Decision Role: 每架飞机提交油量、预计时间和附近交通，塔台另问降落顺序。
- Core Benefit: 把多飞机冲突做成可观察的决策场景，每次请求都能展开查看。

### jev-playground
- Repository: https://github.com/Little-Planet-Labs/jev-playground
- Category: Decision Tools
- Overview (EN): jev-playground: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 在网页里贴一段状态、添加选择题或评分题，直接看 Jev 的答案和概率分布。
- Jev Decision Role: 把多道 Noul、Choice、Score 问题放进同一次请求。
- Core Benefit: 不用先写业务代码就能调整问题、候选和评分标准。

### laravel-typesafe-jev
- Repository: https://github.com/Butochnikov/laravel-typesafe-jev
- Category: MCP & Integrations
- Overview (EN): laravel-typesafe-jev: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 把 Jev 接入 Laravel，提供配置、依赖注入、Facade 和可记录请求的测试替身。
- Jev Decision Role: 复用社区 PHP SDK 发出三类判断请求，保留类型、异步 Promise 和异常。
- Core Benefit: Laravel 服务与队列任务可以沿用自己的配置和测试方式。

### jev-go
- Repository: https://github.com/guillemus/jev-go
- Category: MCP & Integrations
- Overview (EN): jev-go: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 一个接口很小的社区 Go SDK，用来发 Jev 请求和读取模型列表。
- Jev Decision Role: 将 Go 的 Choice、Noul、Score 问题转成 System One API 请求。
- Core Benefit: 给 Go 项目提供最基本的 Jev 调用入口。

### ask-jev
- Repository: https://github.com/omni-/ask-jev
- Category: CLI & Pipelines
- Overview (EN): ask-jev: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 在 Windows Codex 会话里输入 :jev，让它按已记录的执行证据做一次检查。
- Jev Decision Role: 向 Jev 批量询问测试、执行与证据是否支持结论，也可单独问一个是非题。
- Core Benefit: 把“Agent 说做了”与日志里实际留下的证据分开检查。

### jev-code
- Repository: https://github.com/rhighs/jev-code
- Category: CLI & Pipelines
- Overview (EN): jev-code: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 一个实验性编程终端：让 Jev 做受限选择，逐步拼出 AST，并调用本地工具。
- Jev Decision Role: 在有限语法和工具候选里选下一步，代码负责组合成 Python、Bash 等输出。
- Core Benefit: 能观察结构化决策如何参与代码生成和执行。

### jev-doom-agent
- Repository: https://github.com/lukaske/jev-doom-agent
- Category: High-Frequency & Simulation
- Overview (EN): jev-doom-agent: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 在浏览器里跑两份 Doom 引擎，让 Jev 根据游戏状态选择战术动作。
- Jev Decision Role: 读取结构化血量、弹药和可见目标，从战术宏中选一项，再交给本地控制器执行。
- Core Benefit: 能在相同起点对照不同策略，并查看每次模型决策。

### jev-rerank-bench
- Repository: https://github.com/anessbelbati/jev-rerank-bench
- Category: Decision Tools
- Overview (EN): jev-rerank-bench: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 把搜索出来的三十段文字交给 Jev，测试它能否像专用 reranker 一样排出相关内容。
- Jev Decision Role: 用相关性评分、是非判断或 Choice 对相同候选重排，并记录原始响应。
- Core Benefit: 能按数据集查看质量、延迟、成本和置信区间。

### jev-predict-skill
- Repository: https://github.com/DanielKillenberger/jev-predict-skill
- Category: Decision Tools
- Overview (EN): jev-predict-skill: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 一个 Agent skill：读另一个 skill 的规则和现有证据，预测它下一次会选哪个结论。
- Jev Decision Role: 先判断任务能否变成有限选项，再从目标 skill 原有结论里选一项。
- Core Benefit: 在不执行目标 skill 的情况下，先得到一次可检查的候选判断。

### jev-trade
- Repository: https://github.com/aowang-ai/jev-trade
- Category: Domain & Vertical Tools
- Overview (EN): High-frequency perp trader on Hyperliquid with live dashboard (jev-trade.com). Evaluates order book every tick to trade.
- Overview (ZH): 在 Hyperliquid 永续合约上做高频交易（带实时看板 jev-trade.com）。每个 Tick 读取行情，由 Jev 决定开仓平仓。
- Jev Decision Role: 每个币种用独立状态调用 Jev，秒级评估盘口深度与失衡，决定多空方向与挂单。
- Core Benefit: 实现在去中心化衍生品上的亚秒级自动化交易，并配有完整的 Web 实时监控看板。

### jevsume
- Repository: https://github.com/unownone/jevsume
- Category: Domain & Vertical Tools
- Overview (EN): jevsume: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给简历做一次结构化体检。既检查措辞、结构和机器可读性，也能对照具体职位描述，看这份简历是否匹配。
- Jev Decision Role: 对提取出的简历文本与职位要求逐项判断和打分，再由 Worker 汇总成页面里的评审结果。
- Core Benefit: 能保存输入、问题与输出，方便回看每次评审；没有 API key 时运行的是模拟结果。

### jev_ampcode
- Repository: https://github.com/thesammykins/jev_ampcode
- Category: MCP & Integrations
- Overview (EN): jev_ampcode: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 给 Amp 一个比较方案的小工具。把备选方案、证据和偏好摆出来，让 Jev 帮忙选。
- Jev Decision Role: 在明确候选中分配选择概率，并分别检查各方案是否符合要求。
- Core Benefit: 把方案取舍记录成可对照的判断；结果只作建议，不授权执行。

### jev-tool-permissions
- Repository: https://github.com/NicolasMontone/jev-tool-permissions
- Category: Security & Guardrails
- Overview (EN): jev-tool-permissions: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Vercel AI SDK 的工具调用加门禁，顺便收起本轮用不到的工具。
- Jev Decision Role: 确定性规则先检查，再让 Jev 判断自动放行、问人或拦截，以及工具是否相关。
- Core Benefit: 把权限检查和工具筛选接进现有 Agent；请求失败时转人工或保留工具。

### jevthoven
- Repository: https://github.com/cocktailpeanut/jevthoven
- Category: Creative Tools
- Overview (EN): jevthoven: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 用一句话描述想听的音乐，让 Jev 逐小节选音符，生成能编辑、播放和导出的多轨 MIDI。
- Jev Decision Role: 依次选择曲式、乐器、和弦、节奏和整小节模式，本地程序把选择变成音符。
- Core Benefit: 保留每一步选曲依据，也能手动修改并导出 MIDI。

### doom-jev
- Repository: https://github.com/AmoghCreator/doom-jev
- Category: High-Frequency & Simulation
- Overview (EN): doom-jev: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 让 Jev 玩 Doom：看结构化战况，决定往哪走、瞄谁和什么时候开火。
- Jev Decision Role: 选择宏观目标、敌人、移动、转向、跳跃与开火；几何逻辑接手细微瞄准。
- Core Benefit: 把游戏运行和网络推理分开，不必每一帧都等待模型。

### jev-context
- Repository: https://github.com/zbush/jev-context
- Category: Context GC & Filter
- Overview (EN): jev-context: Prunes extraneous tool outputs, logs, and grep results before feeding LLM context.
- Overview (ZH): 给 Codex 的代码搜索加一道筛子。先用 ripgrep 找候选，再只把 Jev 判为相关的片段送回来。
- Jev Decision Role: 逐段判断代码是否与问题相关，只返回 Yes 片段，并记录筛选前后的文本。
- Core Benefit: 能按指定 tokenizer 核对取回文本的变化；不等同整项任务的成本节省。

### ui-generator-instinct-jev
- Repository: https://github.com/joevidev/ui-generator-instinct-jev
- Category: Creative Tools
- Overview (EN): ui-generator-instinct-jev: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 描述想要的界面，让 Jev 从现成组件里选类型、字段和样式，再由程序拼出来。
- Jev Decision Role: 从有限候选中选择组件家族、具体组件、字段类型与外观；分不清时追加比较。
- Core Benefit: 输出已有组件组成的界面和可复制 JSX，不依赖模型随手编写代码。

### jev-use
- Repository: https://github.com/vlad-terin/jev-use
- Category: Browser & OS Action
- Overview (EN): jev-use: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 让 Codex 先定目标，再让 Jev 连续选控件。浏览器和桌面动作仍由 Codex 的现有工具执行。
- Jev Decision Role: 根据观察选择目标控件；本地循环执行、检查结果，遇到例外再交回主 Agent。
- Core Benefit: 把多个常规操作收进一次连续流程，减少主模型逐步接力。

### is-odd-jev
- Repository: https://github.com/alxcrt/is-odd-jev
- Category: CLI & Pipelines
- Overview (EN): is-odd-jev: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 一个刻意小题大做的实验：把数字交给 Jev，问它是不是奇数，再看返回的概率。
- Jev Decision Role: 用 Noul 回答 Is n odd，本地按概率转成布尔值。
- Core Benefit: 用极小接口示例观察模型判断与确定性计算的差别。

### mobile-jev
- Repository: https://github.com/droidrun/mobile-jev
- Category: Browser & OS Action
- Overview (EN): mobile-jev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 给 Android 手机一个目标，Jev 选应用和控件，Mobilerun 负责打开、点击、输入；网页面板能看过程。
- Jev Decision Role: 一次请求选择操作及兼容目标，本地重新核对控件后通过 Mobilerun 执行。
- Core Benefit: 把移动端动作、观察和执行记录接成可检查的任务循环。

### jev-demo
- Repository: https://github.com/minghanminghan/jev-demo
- Category: Routing & Cost Optimization
- Overview (EN): jev-demo: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 一个客服分流小样：同一轮先问全套分类问题，再沿答案选路径；用户想转人工时就提前交接。
- Jev Decision Role: 并行评估分类树各层，同时判断转人工意愿和挫败程度，再由本地规则选用需要的答案。
- Core Benefit: 把多层分流问题合并询问，避免每走一层就重新请求。

### jev-music-theory-1
- Repository: https://github.com/adammichaelwood/jev-music-theory-1
- Category: Domain & Vertical Tools
- Overview (EN): jev-music-theory-1: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 让 Jev 做和声习题，也让它选和弦弹一段电钢琴。重点是观察它懂哪些乐理、在哪些关系上出错。
- Jev Decision Role: 从有限的声部、音高、时值或根音、和弦类型中选择，代码负责发声和规则评分。
- Core Benefit: 把音乐判断拆成可测的小选择题，并留下失败边界。

### jev-macos-loop
- Repository: https://github.com/jcpsimmons/jev-macos-loop
- Category: Browser & OS Action
- Overview (EN): jev-macos-loop: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 在 Mac 本地识别屏幕文字和控件，把文字选项交给 Jev，再点击它选中的元素。
- Jev Decision Role: Jev 从本地视觉、OCR 与无障碍标签组成的有限元素列表中选择；坐标和输入执行留在 Mac。
- Core Benefit: 让原生界面点击使用可枚举目标，并在执行前复核焦点与元素状态。

### jev-router-playground
- Repository: https://github.com/hugo-alves/jev-router-playground
- Category: Routing & Cost Optimization
- Overview (EN): jev-router-playground: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 把几种模型放进候选池，让 Jev 选一个，再把各模型的实际回答摆出来由你评判。
- Jev Decision Role: 对任务与候选模型描述做适配度判断，本地整理概率并选择候选；可继续调用候选模型作比较。
- Core Benefit: 留下路由选择与人工偏好的对照记录，便于检查选得是否合适。

### jevscript
- Repository: https://github.com/amberwhitehead/jevscript
- Category: CLI & Pipelines
- Overview (EN): jevscript: Command-line utility embedding Jev decision logic directly into Unix pipes and CI scripts.
- Overview (ZH): 一个把语义判断写进程序语言的早期实验。目前先用手写脚本验证：几个问题合在一起问 Jev 会怎样。
- Jev Decision Role: M0 脚本对同一工单发送 Choice、Noul、Score，比较合并请求与逐题请求。
- Core Benefit: 给后续解释器设计建立接口与批量请求的基线。

### grokskill-jev
- Repository: https://github.com/AE-AlphaEdge/grokskill-jev
- Category: Browser & OS Action
- Overview (EN): grokskill-jev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 把 Jev Ultrafast 接到 Grok Build，复用已打开的 Chrome 标签页，并提供 PowerShell 启动方式。
- Jev Decision Role: 沿用上游：Jev 选择点击、选择或完成动作，需要写文字时再调用小模型。
- Core Benefit: 为 Grok 和 Windows 使用流程补上 skill、启动器及浏览器复用。

### jev-trade
- Repository: https://github.com/Waxmell114514/jev-trade
- Category: Domain & Vertical Tools
- Overview (EN): High-frequency perp trader on Hyperliquid with live dashboard (jev-trade.com). Evaluates order book every tick to trade.
- Overview (ZH): 把 BTC、ETH 的行情变成文字状态，让 Jev 给交易判断，再放进含延迟和费用的模拟撮合里观察。
- Jev Decision Role: 代码先计算市场特征；Jev 回答有限的方向与风险问题，本地策略决定模拟仓位。
- Core Benefit: 把判断、延迟和执行成本放到同一实验记录里比较。

### jev-demos
- Repository: https://github.com/Bud-ro/jev-demos
- Category: High-Frequency & Simulation
- Overview (EN): jev-demos: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 用迷宫考 Jev：只问下一步，或一次问后面很多步，看看它何时撞墙、绕路或反复打转。
- Jev Decision Role: 把迷宫状态与多步候选动作交给 Jev，再由模拟器逐步执行和判分。
- Core Benefit: 用明确的合法移动规则比较不同问法，展示空间推理的局限。

### jevex
- Repository: https://github.com/jvsteiner/jevex
- Category: MCP & Integrations
- Overview (EN): jevex: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): 让 Jev 当流程指挥，普通语言模型只填参数和写最后回答，MCP 工具负责真正做事。
- Jev Decision Role: Jev 选择工具或结束，语言模型填开放内容，Python 验参后再由 Jev批准具体调用。
- Core Benefit: 把选工具、写内容和执行权限拆开，逐步记录两种模型各做了什么。

### jev-experiments
- Repository: https://github.com/mittal-parth/jev-experiments
- Category: High-Frequency & Simulation
- Overview (EN): jev-experiments: Real-time decision engine operating at sub-100ms latency for games, bots, and simulations.
- Overview (ZH): 让 Jev 玩 Chrome 小恐龙和本地射击竞技场：读取结构化状态，选跳跃、移动、瞄准和开火。
- Jev Decision Role: 每个 tick 并行询问动作与相关开关，Python 或页面代码执行物理和碰撞规则。
- Core Benefit: 把持续游戏决策做成带日志和观察面板的可试验循环。

### foreman-jev
- Repository: https://github.com/Shifty-Eye-Games/foreman-jev
- Category: Codebase & Graph Pathfinding
- Overview (EN): foreman-jev: Semantic exploration engine for codebases and knowledge graphs using beam search.
- Overview (ZH): 给 Codex 工人配一个 Jev 监督员。它评估进展，但完成前还必须跑程序员指定的验收命令。
- Jev Decision Role: 对工作状态和证据做进展与完成判断，本地运行验收命令并检查源码在验证期间是否变化。
- Core Benefit: 把模型判断和实际命令结果一并留档，方便检查任务是否真的收尾。

### jevlogs
- Repository: https://github.com/reachjalil/jevlogs
- Category: Context GC & Filter
- Overview (EN): jevlogs: Prunes extraneous tool outputs, logs, and grep results before feeding LLM context.
- Overview (ZH): 日志照样归档，先让 Jev 挑出值得继续分析的部分，再决定哪些送给更重的模型。
- Jev Decision Role: 评估诊断价值、优先级与可行动概率，本地规则注释日志或路由到分析分支。
- Core Benefit: 在保留原始日志链路的同时，把后续分析注意力集中到高价值事件。

### ha-conversation-jev
- Repository: https://github.com/luxus/ha-conversation-jev
- Category: Domain & Vertical Tools
- Overview (EN): ha-conversation-jev: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Home Assistant 语音入口做分流：简单灯光命令直接走设备服务，其余交给 Grok 对话助手。
- Jev Decision Role: Jev 把语句分成 fast_service、grok 或 reject，并结合本地目标匹配与置信度门槛处理。
- Core Benefit: 让支持的开灯、关灯、调亮度命令走明确的设备操作路径。

### ai
- Repository: https://github.com/vercel/ai
- Category: SDK & Integrations
- Overview (EN): ai: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): AI SDK 的可选 TypeSafe provider：用统一 evaluate 接口一次问 Jev 多个选择、评分和是非问题。
- Jev Decision Role: 把共享状态与问题发到 System One API，将原生概率、分数和用量映射回 SDK 结果。
- Core Benefit: 已有 AI SDK 项目可以沿用接口接入结构化评估。

### langchain
- Repository: https://github.com/langchain-ai/langchain
- Category: SDK & Integrations
- Overview (EN): langchain: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Python LangChain 流程加一个可选 Jev 分类节点，返回类别、概率和等级评分。
- Jev Decision Role: TypeSafeClassifier 将 JSON 状态及类型化问题发送到 /v1/systemone，支持同步与异步 Runnable 调用。
- Core Benefit: 把结构化判断接进已有 LangChain 流程与追踪接口。

### oh-my-pi
- Repository: https://github.com/can1357/oh-my-pi
- Category: SDK & Integrations
- Overview (EN): oh-my-pi: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Oh My Pi 可选用 Jev 处理小判断：该用多深思考、是否意外停下，以及 Git 暂存相关分类。
- Jev Decision Role: 统一 judgment 接口在提供 TypeSafe 凭据后调用 Jev；结构化问题返回概率供本地策略使用。
- Core Benefit: 把小型分类任务集中到专门接口，保留替换后端的能力。

### eliza
- Repository: https://github.com/elizaOS/eliza
- Category: SDK & Integrations
- Overview (EN): eliza: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Eliza 源码内有一个可选 TypeSafe HTTP 适配器，供明确调用时发送结构化判断请求。
- Jev Decision Role: 调用方提供模型、状态和问题，systemOne 校验输入输出后请求 Jev。
- Core Benefit: 为需要接入的代码提供受约束的请求与响应验证。

### composio
- Repository: https://github.com/ComposioHQ/composio
- Category: SDK & Integrations
- Overview (EN): composio: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Composio 工具箱接一个可选 Jev 决策层：从工具列表挑工具，填能枚举的参数，其余交给调用方补。
- Jev Decision Role: 把工具与封闭参数编译成类型化问题，返回完整调用、部分调用或弃权；随后显式 execute。
- Core Benefit: 复用已有 Composio 工具，同时把缺少的参数和置信度明确暴露出来。

### pydantic-ai
- Repository: https://github.com/pydantic/pydantic-ai
- Category: SDK & Integrations
- Overview (EN): pydantic-ai: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Pydantic AI 的可选 Jev 模型：把输出模型里的布尔和枚举字段变成问题，拿回符合类型的判断。
- Jev Decision Role: TypeSafeModel 将支持的 output_type 字段编译为类型化问题，经 TypeSafeProvider 请求 API 后还原输出。
- Core Benefit: 决策型 Agent 可以复用 Pydantic 输出定义，并与其他模型作对照。

### litellm
- Repository: https://github.com/BerriAI/litellm
- Category: Routing & Cost Optimization
- Overview (EN): litellm: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): LiteLLM 的复杂度路由可选用 Jev：先判断请求需要哪个模型档，再交给路由策略处理。
- Jev Decision Role: Jev Choice 在配置的模型档位中选一个，HTTP 客户端返回概率与置信度供复杂度路由使用。
- Core Benefit: 把请求难度判断接进现有模型分层与路由配置。

### langchainjs
- Repository: https://github.com/langchain-ai/langchainjs
- Category: SDK & Integrations
- Overview (EN): langchainjs: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): LangChain.js 的可选 Jev 分类器：给状态配上问题，返回可直接在程序里使用的类别和评分。
- Jev Decision Role: TypeSafeClassifier 按 Runnable 接口发送共享状态与 Choice、Noul、Score，校验并整理回答。
- Core Benefit: TypeScript 工作流可以直接组合结构化判断节点。

### firstmate
- Repository: https://github.com/kunchenguid/firstmate
- Category: Routing & Cost Optimization
- Overview (EN): firstmate: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Firstmate 可选用 Jev 看任务简报，匹配派工规则，再由本地规则选出具体代理配置。
- Jev Decision Role: 对任务与候选派工规则做一次 Choice；置信度、审批、配额和候选排序留在 shell 逻辑。
- Core Benefit: 把规则匹配变成一个可记录的步骤，并在不确定时交回决策。

### openchamber
- Repository: https://github.com/openchamber/openchamber
- Category: Routing & Cost Optimization
- Overview (EN): openchamber: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): OpenChamber 可选开启自动模型路由：Jev 看消息属于哪类任务，再使用该类绑定的模型和思考档位。
- Jev Decision Role: Choice 判断任务类别；另可用 Noul 判断自动批准的权限是否应留给用户确认。
- Core Benefit: 把模型选择和权限提示接进现有会话界面，并记录失败回退原因。

### ax
- Repository: https://github.com/ax-llm/ax
- Category: SDK & Integrations
- Overview (EN): ax: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Ax 的可选 TypeSafe 接口：用布尔或固定类别签名调用 Jev，也可直接读取原生概率与评分。
- Jev Decision Role: 适配器将支持的输出字段变成 Jev 问题，原生 client 的 systemOne 负责发送和验证回答。
- Core Benefit: 结构化决策可以和 Ax 的生成式步骤显式组合。

### agentgateway
- Repository: https://github.com/agentgateway/agentgateway
- Category: Security & Guardrails
- Overview (EN): agentgateway: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Agentgateway 的一个可选护栏示例：让 Jev 检查模型请求和回复里的越狱、有害内容及秘密泄露。
- Jev Decision Role: Bun webhook 通过网关调用 Jev 评分，本地阈值达到拒绝等级就返回 HTTP 403。
- Core Benefit: 把内容检查挂到已有网关的请求和响应路径。

### omg.dev
- Repository: https://github.com/BennyKok/omg.dev
- Category: Browser & OS Action
- Overview (EN): omg.dev: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): omg.dev 的移动端测试脚本可用 Jev 看无障碍树：选下一控件，判断步骤是否完成或已经走不通。
- Jev Decision Role: 测试辅助 judge 将界面树与 Noul、Choice 问题发往 Jev，精确字符串断言仍由代码控制。
- Core Benefit: 让端到端测试在界面略有变化时多一个语义判断信号。

### pi-fabric
- Repository: https://github.com/monotykamary/pi-fabric
- Category: SDK & Decision Frameworks
- Overview (EN): pi-fabric: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Pi 的工具运行时加上可编程的 Jev 决策循环。先写好观察、判断和执行步骤，再让它按预算在前台或后台运行。
- Jev Decision Role: 对程序提交的状态做单选、是非判断或评分；循环和动作执行由本地程序控制。
- Core Benefit: 适合把反复发生的小判断写成可复用流程；Jev 是需要配置的可选能力。

### latitude-llm
- Repository: https://github.com/latitude-dev/latitude-llm
- Category: Evaluation & Observability
- Overview (EN): latitude-llm: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Latitude 的对话检查流程加一个旁路观察员。可选调用 Jev，记录它认为哪些检查值得运行，先与原流程比较。
- Jev Decision Role: 对对话状态回答一个是非问题，再由阈值记为“会运行”或“会跳过”。
- Core Benefit: 能积累模型决策与用量记录；当前是默认关闭的影子试验，不改变现有检查结果。

### instructor-php
- Repository: https://github.com/cognesy/instructor-php
- Category: SDK & Decision Frameworks
- Overview (EN): instructor-php: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 让 PHP 应用通过统一的 Decision 接口使用 Jev。把业务状态和判断题交进去，拿回程序可处理的选项、分数与概率。
- Jev Decision Role: 通过 Polyglot 的 TypeSafe 驱动调用 Jev，对调用方指定的问题作答。
- Core Benefit: 现有 PHP 项目可以复用配置、错误处理和事件记录；这是新增的决策模型支持。

### atomic
- Repository: https://github.com/bastani-inc/atomic
- Category: Routing & Cost Optimization
- Overview (EN): atomic: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): Atomic 编程 Agent 的可选决策后端。路由需要在给定选项中做判断时，可以交给 Jev；写代码仍由常规模型完成。
- Jev Decision Role: 把结构化选择题编译为 Jev Choice 请求，并校验返回选项与概率分布。
- Core Benefit: 把小范围选择从聊天生成里分出来，保留明确的候选和响应校验。

### WrongStack
- Repository: https://github.com/WrongStack/WrongStack
- Category: Routing & Cost Optimization
- Overview (EN): WrongStack: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 WrongStack 编程 Agent 增加一个可选分派助手。遇到多个相近的专长 Agent 时，用 Jev 判断谁更适合当前任务。
- Jev Decision Role: 从候选角色中选一个，同时判断有没有任何候选真正适合；不合适时交回原有策略。
- Core Benefit: 分派结果带有可检查的概率，也能明确表示没有合适人选。

### celesto
- Repository: https://github.com/CelestoAI/celesto
- Category: Codebase & Graph Pathfinding
- Overview (EN): celesto: Semantic exploration engine for codebases and knowledge graphs using beam search.
- Overview (ZH): Celesto 仓库里的 Jev 代码审查示例。先整理 PR 的疑似问题，再让 Jev 检查它是否由本次修改引入、是否有证据、是否值得修。
- Jev Decision Role: 对每条候选发现分别回答“是、否、证据不足”，由代码合成保留或退回补证据的结果。
- Core Benefit: 把审查意见拆成可逐项追问的判断；这是示例流程，不代表整个 Celesto 默认使用 Jev。

### req_llm
- Repository: https://github.com/agentjido/req_llm
- Category: SDK & Decision Frameworks
- Overview (EN): req_llm: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 在 Elixir 的 ReqLLM 里接入 Jev 判断题。使用 evaluate 接口提交状态与问题，返回答案和概率；聊天生成仍走其他模型。
- Jev Decision Role: TypeSafe provider 把 evaluate 请求发到 System One，并统一是非、单选和评分结果。
- Core Benefit: Elixir 应用能沿用同一套鉴权、重试、用量与遥测接口。

### vellum-assistant
- Repository: https://github.com/vellum-ai/vellum-assistant
- Category: MCP & Integrations
- Overview (EN): vellum-assistant: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): Vellum Assistant 增加了一个 Jev provider。选用它后，可以把对话状态或明确的问题包交给 Jev，获得结构化判断。
- Jev Decision Role: 调用原生 System One 接口回答命名问题，并通过适配层把结果送回现有助手接口。
- Core Benefit: 能在同一个助手框架里试用 Jev；这证明兼容接入，不代表助手默认由 Jev 驱动。

### vexjoy-agent
- Repository: https://github.com/notque/vexjoy-agent
- Category: Routing & Cost Optimization
- Overview (EN): vexjoy-agent: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 VexJoy 的任务分派增加一条 Jev 路线。输入需求后，判断该选哪位专长 Agent、哪项技能和哪条工作流。
- Jev Decision Role: 先判断是否需要路由，再从实际清单中选择候选；非法选择或调用失败交回原有流程。
- Core Benefit: 提供可对照的路由实验入口；项目文档明确说当前评测不足以把它提升为默认路线。

### aiavatarkit
- Repository: https://github.com/uezo/aiavatarkit
- Category: Voice & Conversation
- Overview (EN): aiavatarkit: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 让语音角色少一点抢话。AIAvatarKit 可用 Jev 判断用户是真的说完了，还是停下来想一想、准备继续说。
- Jev Decision Role: 结合转写文本和停顿时长，估计是否应该继续等待，再由配置决定何时结束这一轮。
- Core Benefit: 为固定静音计时补充语义线索；这是可选的语音轮次判断组件。

### taskuary
- Repository: https://github.com/ldbumble/taskuary
- Category: MCP & Integrations
- Overview (EN): taskuary: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): Taskuary 把消息、任务和 Agent 工作汇到一起，并提供可选 Jev 判断器，对一次运行的状态检查是否满足用户设定的条件。
- Jev Decision Role: 把同一份运行状态与多条条件提交为是非问题，返回布尔结果及原始概率。
- Core Benefit: 让任务路由使用可查看的判断信号；Jev 不承担助手的聊天或写作。

### orchestkit
- Repository: https://github.com/yonatangross/orchestkit
- Category: Classification & Taxonomy
- Overview (EN): orchestkit: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): OrchestKit 可选用 Jev 给编程会话分类：这是排错、开发还是其他工作。达到设定阈值时，用分类结果决定会话标识颜色。
- Jev Decision Role: 对会话开头的信息在固定类别中做单选；不确定或失败时继续使用原有 Haiku 分类。
- Core Benefit: 能并排记录两种分类结果，也能先用影子模式观察；默认不会调用 Jev。

### skillbox
- Repository: https://github.com/kitze/skillbox
- Category: Context GC & Filter
- Overview (EN): skillbox: Prunes extraneous tool outputs, logs, and grep results before feeding LLM context.
- Overview (ZH): 自建一个有版本管理的 Agent 技能库，还能选配 Jev 推荐：告诉它当前任务，从你有权限使用的技能里挑更相关的。
- Jev Decision Role: 对候选技能逐项评估任务相关度，再由应用整理推荐结果。
- Core Benefit: 技能查找不只依赖关键词；未配置或调用失败时仍可使用确定性搜索。

### interlinked-cli
- Repository: https://github.com/QuentinCody/interlinked-cli
- Category: Security & Guardrails
- Overview (EN): interlinked-cli: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给编程 Agent 的本地规则检查加上可选 Jev 评分。它为检查流程提供补充判断，保留确定性规则作为基础。
- Jev Decision Role: 对传入状态回答是非、选择或评分问题，供上层策略使用；失败时返回“未测量”。
- Core Benefit: 可以对补充判断设置超时和用量上限；Jev 分数本身不是权限许可。

### effect-agent
- Repository: https://github.com/danieljvdm/effect-agent
- Category: SDK & Decision Frameworks
- Overview (EN): effect-agent: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 在 Effect Agent 里接入 Jev 决策模型。TypeScript 程序可以用带类型的问题集做判断，也能把它用于可选的模型选择。
- Jev Decision Role: TypeSafeDecisionModel 通过 TypeSafeClient 调用 Jev，并把概率、单选与评分结果接回 Effect。
- Core Benefit: 复用 Effect 的配置、类型和错误处理；重试与超时由应用自己设置。

### pi-warden
- Repository: https://github.com/DevMortimer/pi-warden
- Category: Security & Guardrails
- Overview (EN): pi-warden: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 给 Pi 编程 Agent 加一位规则监督员。写文件时检查是否违反项目约定，执行命令前评估危险操作，把问题直接反馈给 Agent。
- Jev Decision Role: 通过 Jev 判断改动是否违背规则、是否偏离任务或不可逆，再与本地模式规则合并处理。
- Core Benefit: 让规则提醒进入实际工具流程；多数问题是提示，少数破坏性操作才会被拦下。

### advocaat
- Repository: https://github.com/pithings/advocaat
- Category: SDK & Decision Frameworks
- Overview (EN): advocaat: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 用简短的 TypeScript 调用向 Jev 提问。把同一份数据里的多个判断一次写好，直接拿到概率、选项和分数。
- Jev Decision Role: 将带类型的问题转成 System One 请求，并把命名答案映射回调用方。
- Core Benefit: 减少手写请求和解析代码，也支持通过 Vercel 网关调用。

### synkora-ai
- Repository: https://github.com/getsynkora/synkora-ai
- Category: MCP & Integrations
- Overview (EN): synkora-ai: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): Synkora Agent 平台提供 TypeSafe 客户端，可把业务数据交给 Jev 做分类、评分或是非判断，作为工作流中的一项能力。
- Jev Decision Role: 把调用方的 question、options 和 levels 转成 Jev 的题型与标准，再调用 System One。
- Core Benefit: 应用可以沿用统一的凭据和结果格式；这是兼容接入，不代表整个平台默认使用 Jev。

### ai
- Repository: https://github.com/hackclub/ai
- Category: MCP & Integrations
- Overview (EN): ai: Exposes Jev low-latency decision primitives via the Model Context Protocol (MCP).
- Overview (ZH): Hack Club 的 AI 代理增加了 Jev 转发接口。获准用户可通过统一入口提交判断题，同时走现有的鉴权、限额和用量记录。
- Jev Decision Role: 代理把状态与问题转发给 TypeSafe，再将 Jev 的结构化答案返回调用方。
- Core Benefit: 接入已有账户和配额体系；固定版本里的 Jev 通道处于受开关控制的封闭测试。

### unclutter
- Repository: https://github.com/kitze/unclutter
- Category: Browser & OS Action
- Overview (EN): unclutter: Automates browser or desktop interactions by turning UI state into discrete choices.
- Overview (ZH): 给网页做视觉减法的浏览器扩展。让 Jev 识别广告、促销和订阅弹窗，再把可复用的隐藏规则存在本地。
- Jev Decision Role: 对页面元素候选做分类，只让通过阈值的非必要元素进入隐藏规则，不确定的保留。
- Core Benefit: 常见页面可以复用规则，隐藏操作可撤销；它不阻止追踪请求，也不代替用户处理同意选项。

### openai-scala-client
- Repository: https://github.com/cequence-io/openai-scala-client
- Category: SDK & Decision Frameworks
- Overview (EN): openai-scala-client: Domain-tailored decision automation for quantitative trading, auditing, and specialized operations.
- Overview (ZH): 让 Scala 应用也能接入 Jev。这个多模型客户端新增了独立 TypeSafe 模块，用状态和判断题获取结构化答案。
- Jev Decision Role: 调用 System One 返回单选、评分和是非概率；可把受支持的封闭 JSON Schema 转成判断题。
- Core Benefit: 能沿用 Scala 的异步接口和错误处理；Jev 是额外支持的 provider，不承担普通聊天生成。

### ego-jev
- Repository: https://github.com/phd-peter/ego-jev
- Category: Browser & OS Action
- Overview (EN): Integrates Jev with Ego Lite browser agent. Reads semantic snapshots to decide DOM clicks and wheel scrolls, delegating text entry to LLMs.
- Overview (ZH): 把 Jev 接入 Ego Lite 浏览器。读取轻量语义快照，由 Jev 决定点哪个控件、怎么滚轮，需要输文字时才叫大模型。
- Jev Decision Role: 读取当前页面的动作空间候选，在单次请求里选定目标控件与操作类型；由 ego-lite 本地执行。
- Core Benefit: 用轻量语义快照替代昂贵的视觉多模态，5 步跑完维基百科复杂导航，延迟极低。

### jev-visual
- Repository: https://github.com/hr98w/jev-visual
- Category: SDK & Decision Frameworks
- Overview (EN): Local Jev-like visual inference experiment on Apple Silicon Mac. Scores and classifies single images across multiple questions with 3 playable game demos.
- Overview (ZH): 在 Apple Silicon Mac 本地跑的视觉版 Jev 实验。对单张图片做选择、打分与是非判断，自带 3 个本地视觉游戏 Demo。
- Jev Decision Role: 复用图像上下文，读取模型 logits 直接为候选答案打分，由本地代码拼装结构化结果。
- Core Benefit: 把 Jev 的多问题单次打分范式拓展到端侧视觉场景，本地秒级跑通分拣与体感游戏。

### bluenoise
- Repository: https://github.com/rokcso/bluenoise
- Category: Context GC & Filter
- Overview (EN): X/Twitter browser extension filtering noise. Uses local rules first, batching ambiguous replies to Jev for noise probability scoring (filters at >=0.9).
- Overview (ZH): 推特/X 浏览器的降噪过滤扩展。本地规则过滤垃圾，疑难回复成批交给 Jev 判定噪音概率（>=0.9 则隐藏）。
- Jev Decision Role: 对未命中本地规则的长尾回复，单次最多批量提交 25 条询问 noul 噪音概率，由代码决定是否隐藏。
- Core Benefit: 不调用任何 X 官方 API，用本地混合架构实现毫秒级社交媒体时间线清洁。

### jev-gomoku
- Repository: https://github.com/XieChengYuan/jev-gomoku
- Live Demo: https://xiechengyuan.github.io/jev-gomoku/
- Category: High-Frequency & Simulation
- Overview (EN): Dual-Jev 9x9 Gomoku workbench evaluating how input representations affect placement decisions, featuring replay and live play.
- Overview (ZH): 弈瞬：双 Jev 玩家在九宫格五子棋中对战，比较不同输入信息对落子决策的影响，并逐手展示真实请求与返回。
- Jev Decision Role: 每盘每手回答一个 Choice 问题：根据棋盘、规则、颜色和合法候选选择落子点，由本地代码校验执行。
- Core Benefit: 逐手展示真实请求与返回概率，支持免 API Key 真实对局回放与 OpenRouter 自定义对战，支持 JSON 导出。

