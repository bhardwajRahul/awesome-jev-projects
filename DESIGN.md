# Awesome Jev — JEV-01 机械决策工作台

本设计服务于发现、理解和比较 Jev 的实际接入方式。首屏是一张有刻度、发条助手与实体卡仓的工程工作台；下方用清晰的搜索和项目列表完成发现。抽卡提供触觉反馈，赞助保持明确标注。页面保持中、英、日、韩一致的结构。

## 色卡与材料

唯一颜色来源是 `src/styles/theme.css`。组件读取语义角色，不自行决定浅色或深色。九级中性色分别承担画布、面板、凹面、边界和文字；`--night-*` 与全局暗色同源，供抽卡浮层保持暗色。

| 角色 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| Canvas | `#fafaf8` | `#101310` | 暖灰图纸 / 黑曜石基底 |
| Surface | `#f3f4ef` | `#1a201a` | 分发台、赞助与辅助面板 |
| Raised | `#fcfcf9` | `#232b22` | 项目卡、表单控件 |
| Text | `#20271e` | `#edf1e8` | 标题和核心说明 |
| Muted | `#53614d` | `#b6c2ad` | 作者、来源、辅助说明 |
| Quiet | `#5d6858` | `#a6b39f` | 次级元信息和占位文字 |
| Electric lime | `#d7fa91` | `#d7fa91` | 选择结果、激活状态与少量主行动 |
| Brass | `#7b5b28` | `#d9bc82` | 日期印章、SSR 与关注度标识 |

浅色模式的绿色文字使用深绿 `--accent-text`，不用 lime 写浅底文字。警告、错误、代码和主按钮各有成对的前景／背景角色。对比测试解析实际 CSS 变量，要求正文及辅助文字至少 4.5:1、焦点至少 3:1。

材质另有统一角色：`--metal-top / --metal-bottom` 控制金属受光面，`--material-edge` 控制顶部倒角反光，`--shadow-material` 同时包含内高光、接触阴影与环境阴影，`--shadow-recess` 表示卡槽和决策区的凹陷。浅、深色分别调节亮度与遮蔽，不给每个组件随意挑阴影。

颜色切换同步完成，不对文字和背景分别渐变，避免中间帧浅字浅底。机械反馈使用位移、旋转和阴影；出卡按压阶段为 120ms。所有循环和弹性动作遵守 reduced-motion。

## 页面节奏

- **Header：** Sponsor、Agent Skill 与 Star 共享连体金属胶囊和内部分隔；提交项目是唯一实心主按钮；主题与语言保留低权重工具槽，地球图标与语言位于最右端。手机两行重排。
- **Hero：** 左侧有 6% 点阵、工程刻度和目录状态胶囊，标题仍明确分工；下方的 Jevy 与右侧卡仓形成两处不同体量的机械实体。发现、代码和决策流程三 Tab 保留。状态胶囊显示动态目录数，不虚构模型 API 的实时运行状态。
- **Jevy：** 原创手写 SVG，由黄铜仪表眼眶、石墨铸壳、关节钳手、胸齿轮、发条钥匙与晶体管天线组成。天线每 3 秒闪烁，齿轮缓转；Hover 与键盘聚焦有弹性反馈，点击可上发条。离屏及后台暂停循环，手机缩为侧方微型助手。
- **卡牌分发仓：** 铭牌 `JEV-01 // TACTILE DISPATCHER`、倒角机壳、实体 LED、凹陷卡槽、纸卡分层与黄铜印章共同表达厚度。Hover 拔出顶层卡片 8px；点击完成 120ms 按压并打开抽卡浮层。手机保留真实卡仓形态，减少内部细节密度。
- **资料带：** 动态显示已审校项目、架构分类、主题标签和更新时间。全站项目实行严格审核准入，不包含未核验记录。没有把第三方大型仓库 Stars 总和当作本站流量或 Jev 采用量。
- **探索区：** 桌面 208px 侧栏＋双列结果，768px 以下为横滑分类和单列项目。搜索先于招商空位；未售赞助位压成单行，付费内容继续明确标识。
- **项目卡：** 顶部切边反光与接触／环境双层阴影赋予实体重量，Jev 决策点嵌入凹面。一句话用途仍主导扫读，来源／许可／收藏／Clone 操作保持可达，悬停上移 2px。
- **日历与页脚：** 日历使用小黄铜印章，手机保留 36px 微条；页脚作为稳定的源码、赞助和 Agent 入口。

## 取舍与文案

机械感由实际构造表达：外壳受光与遮蔽、卡槽深度、卡纸厚度，以及 Jevy 的可辨认关节和传动部件。实体、光源与动作方向相互对应。全部使用手写 SVG、CSS 与 React 状态，无新增图形引擎或动画库。Inter 用于界面和正文；JetBrains Mono 用于代码、铭牌和数值。

标语是“把思考留给大模型，把选择题交给 Jev。”定位为社区维护的 **System-1 Agent Architecture Radar**。不使用无法证实的“全球首个”，也不把厂商特定工作流的延迟范围写成本站对所有项目的保证。[TypeSafe 官方说明](https://typesafe.ai/blog/introducing-system-one-models-and-jev)给出的范围与测试条件仍应由来源解释。

## 参考与边界

[Geist 颜色规范](https://vercel.com/geist/colors)启发语义色与状态分层；[Linear Method](https://linear.app/method)强调具体问题与清晰工作路径；[Stripe Press](https://press.stripe.com/)提供编辑式层级参考；[Apple HIG](https://developer.apple.com/design/human-interface-guidelines/)支持可读、一致的控件与状态；[Machinarium](https://amanita-design.net/games/machinarium.html)只作为机械温度的参考，未使用其角色、图形或材质。

社交分享图的权威源为手写 `public/og-card.svg`，PNG 为该向量源的导出，不是生成式图片。macOS 可用 `sips -s format png public/og-card.svg --out public/og-card.png` 重建；它不进入页面首屏资源。更新时同步 PNG 和图片版本参数。

记录实际首屏 JS＋CSS 的 raw 与 gzip 差值，不以源文件大小代替生产体积。代码与数据验证、桌面／移动浅深色截图、真实浏览器对比度、四语和 `Jev-cu` 搜索回归是发版条件。性能数字限定于实际测试设备，不作为全设备 60FPS 保证。
